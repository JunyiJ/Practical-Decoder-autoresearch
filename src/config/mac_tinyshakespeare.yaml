# src/llm_lab/config/mac_tinyshakespeare.yaml
data:
  path: "data/raw/shakespeare.txt"

model:
  dim: 256
  n_layers: 6
  n_heads: 8
  vocab_size: 50257 # Default GPT-2 vocab
  block_size: 512
  dropout: 0.2
  rope: 1
  norm_type: "rmsnorm"
  # mla specific params
  dimckv: 32
  dimcq: 64
  head_dim_r: 8
  
  # Swappable components
  attn_type: "gqa" # options: mha, gqa, mla
  n_kv_heads: 1    # for GQA: set to 1 or 2
  mlp_type: "dense" # options: dense, moe
  
  moe:
    num_experts: 4
    shared_num_experts: 1
    top_k: 2
    aux_loss_weight: 0.01
    ffn: "swiglu"

training:
  batch_size: 32
  learning_rate: 8e-4
  max_iters: 5000
  weight_decay: 0.1
  grad_clip: 1.0
  device: "mps" # Change to "cuda" on RunPod
  checkpoint_dir: "checkpoints"
  checkpoint_every: 500

hydra:
  job_logging:
    version: 1
    formatters:
      simple:
        format: "[%(asctime)s][%(name)s][%(levelname)s] - %(message)s"
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.runtime.output_dir}/train.log
    root:
      level: INFO
      handlers: [console, file]
    disable_existing_loggers: false
